<%= form_for(@alignment_position) do |f| %>
  <% if @alignment_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alignment_position.errors.count, "error") %> prohibited this alignment_position from being saved:</h2>

      <ul>
      <% @alignment_position.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
