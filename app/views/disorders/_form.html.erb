<%= form_for(@disorder) do |f| %>
  <% if @disorder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@disorder.errors.count, "error") %> prohibited this disorder from being saved:</h2>

      <ul>
      <% @disorder.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
