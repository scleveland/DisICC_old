<h1>Listing intra_residue_contacts</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @intra_residue_contacts.each do |intra_residue_contact| %>
  <tr>
    <td><%= link_to 'Show', intra_residue_contact %></td>
    <td><%= link_to 'Edit', edit_intra_residue_contact_path(intra_residue_contact) %></td>
    <td><%= link_to 'Destroy', intra_residue_contact, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Intra residue contact', new_intra_residue_contact_path %>
