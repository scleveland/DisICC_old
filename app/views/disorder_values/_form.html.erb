<%= form_for(@disorder_value) do |f| %>
  <% if @disorder_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@disorder_value.errors.count, "error") %> prohibited this disorder_value from being saved:</h2>

      <ul>
      <% @disorder_value.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
